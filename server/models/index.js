const User = require('./User');
const Shift = require('./Shift');
const Schedule = require('./Schedule');
const Hospital = require('./Hospital');

module.exports = { User, Shift, Schedule, Hospital };
